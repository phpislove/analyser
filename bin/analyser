#!/usr/bin/env php
<?php

require __DIR__.'/../vendor/autoload.php';

$app = new Symfony\Component\Console\Application('Analyser', 'development');

# add commands here via $app->add(new ...);

exit ($app->run());
